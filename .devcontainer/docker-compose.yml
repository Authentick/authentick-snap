version: '3'
services:
  authserverdev:
      build:
        context: .
        dockerfile: Dockerfile
      volumes:
        - .:/workspace:cached
      command: /bin/sh -c "while sleep 1000; do :; done"
      ports:
        - 80:80
        - 443:443
