# Contribute to the Gatekeeper Snap



## Create a local `.snap`

```bash
snapcraft --enable-experimental-package-repositories
```

## Install a local `.snap`

```bash
snap install --dangerous *.snap
```